<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cat LIpsum</title>
    <%- include("partials/header.ejs") %>
        <link rel="stylesheet" href="/styles/section.css" />
</head>


<body>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.2.1/dist/js/bootstrap.min.js"
        integrity="sha384-B0UglyR+jN6CkvvICOB2joaf5I4l3gm9GU6Hc1og6Ls7i6U/mkkaduKaBhlAXv9k"
        crossorigin="anonymous"></script>
    <div class="d-lg-flex justify-content-center col-lg-12 col-xs-9 container mt-5">
        <div class="col-lg-5 order-lg-last col-xs-9">
            <h3 class="memePhrase">“One reason that cats are happier than people is that they have no newspapers.” --
                Gwendolyn Brooks</h3>
            <img class="justify-content-center meme"
                src="https://images.petpress.net/wp-content/uploads/2022/04/How-Cats-Use-Their-Heads-scaled.jpg?strip=all&lossy=1&w=1324&ssl=1" />
        </div>

        <div class="col-lg-6 col-xs-9">
            <!-- Recommended Daily Posts -->
            <h1 class="titleHeader mt-3">Recommended Daily Posts</h1>
            <div class="container pl-0" id="data-display">
            </div>
        </div>
    </div>
    <script>

        document.addEventListener("DOMContentLoaded", function () {
            var date = new Date().toJSON().slice(0, 10);
            const files = [
                '/database/catIpsum/article1',
                '/database/catIpsum/article3',
                '/database/catIpsum/article4',
                '/database/catIpsum/article5',
            ];
            Promise.all(files.map(file => fetch(file).then(response => response.json())))
                .then(data => {

                    const dataDisplay = document.getElementById("data-display");
                    data.map(element => {
                        if (element.date.length != 0) {
                            date = element.date
                        }
                        const card = document.createElement("div");
                        card.classList.add("card");
                        card.style.width = "100%";
                        card.innerHTML = `
                        <div class="d-flex justify-content-between" id="data-display">
                            <div class="card-body">
                                <h3 class="card-title">${element.title}</h3>
                                <form action="/submit" method="POST" class="card-link" data-id=${element.id} >
                                <input type="hidden" name="articleId" value="${element.id}">
                                <input type="submit" class="view-article btn btn-dark" value="View Article">
                            </form>
                            </div>
                            <div class="card-footer"> 
                                <p class="card-author" id="card-author">By: ${element.author}</p> 
                            <p class="card-date"> Date: ${date}</p> 
                            </div>
                        </div>`

                        dataDisplay.appendChild(card)
                    })



                })
                .catch(error => console.error("Error fetching JSON data:", error));
        });
    </script>

    <div class="col-lg-12">
        <%- include("partials/footer.ejs") %>
    </div>
</body>

</html>